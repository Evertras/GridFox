(function() {
    var app = angular.module('gridFox', ['agCanvas', 'imageToolbar', 'gridToolbar', 'gridCanvas', 'miscButtons']);

    app.controller('mainController', ['$scope', 'agCanvasService', function($scope, agCanvasService) {
        var self = this;
    }]);
})();